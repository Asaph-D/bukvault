/** Notifications locales (cloche) : session JWT expulsée ou refresh impossible. */
export const AUTH_SESSION_EXPIRED_UI_EVENT = 'bookvault-auth-session-expired-ui';

/** Connexion / refresh réussi : retire l’alerte locale « session ». */
export const AUTH_SESSION_RESTORED_UI_EVENT = 'bookvault-auth-session-restored-ui';
